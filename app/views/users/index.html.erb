<h1>All Users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user %>
      Member Since: <%= user.created_at.strftime("%B %Y") %>
    </li>
  <% end %>
</ul>